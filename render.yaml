services:
  - type: web
    name: finance-bro
    runtime: docker
    plan: free
    branch: main
    dockerfilePath: ./Dockerfile
    dockerContext: .
    autoDeploy: true
    envVars:
      - key: OPENAI_API_KEY
        sync: false
