import riskfolio as rp

ax = rp.plot_table(returns=returns,
                   w=pd.Series(weights_max_sharpe),
                   MAR=0,
                   alpha=0.05,
                   ax=None)

Acceptance criteria:
- successfully reuse the returns variable stored in session state. 
- Reuse the existing weight variables for max sharpe ratio portfolio
