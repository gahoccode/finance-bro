import riskfolio as rp

# Method 1: Convert OrderedDict to DataFrame
converted_weights_sharpe = pd.DataFrame.from_dict(weights_max_sharpe, orient='index', columns=['Max Sharpe'])

rp.excel_report(returns,
                w=converted_weights_sharpe,
                rf=0, # could =0 or =risk_free-rate
                alpha=0.05,
                t_factor=252,
                ini_days=1,
                days_per_year=252,
                name="./outputs/report")