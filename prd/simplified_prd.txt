
1. Create another tab @Portfolio_Optimization.py and implement from pypfopt import HRPOpt
hrp = HRPOpt(returns=returns) #create the object
weights = hrp.optimize() #optimize the object. The weights should gives an OrderedDict with type np.float64. For example, OrderedDict([('DHC', np.float64(0.280008567778799)),
             ('FMC', np.float64(0.4150523222546779)),
             ('REE', np.float64(0.30493910996652307))])

2. Create the visualization using from pypfopt import plotting
plotting.plot_dendrogram(hrp, ax=None, show_tickers=True)
All imports should be place on top of the file. 

Acceptance criteria:
Successfully visualize the weights in dendrogram
Resuse existing variable 'returns' to plot dendrogram